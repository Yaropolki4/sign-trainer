//import { add } from "./module";

const unusedVariable = 42;

function add(a, b) {
  return a + b;
}
const a = 5;
// Использование any
function logMessage(message) {
  console.log(message);
}
logMessage('Hello, world!');

[1, 2, 3].map((item) => {
  return item * 2;
});
